const initialState = {
  welcomeList: [
    { name: aa, isTeacher: true },
    { name: bb, isTeacher: false },
    { name: cc, isTeacher: true }
  ]
};

export default function counterReducer(state = initialState, action) {
  return state;
}
